services:
  - type: web
    name: psx-stock-screener
    runtime: python
    buildCommand: "pip install -r requirements.txt || true"
    startCommand: "python server.py"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

